name: GT Drupal SSO
package: GT
type: module
description: Configuration for Georgia Tech's SSO based auth and attributes (if available) for Drupal sites
core_version_requirement: ^9 || ^10

dependencies:
  - drupal:cas
  - drupal:cas_attributes
  - drupal:externalauth
